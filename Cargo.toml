[package]
name = "synth-core"
version = "0.1.0"
edition = "2024"
description = "Core library of the synth modular synthesizer platform."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
serde_yaml_ng = "0.10.0"
